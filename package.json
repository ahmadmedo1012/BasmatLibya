{
  "name": "basmatlibya",
  "private": true,
  "version": "0.1.0",
  "description": "BasmatLibya — Arabic-first digital footprint analyzer",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @basmat/server --filter @basmat/web dev",
    "build": "pnpm --filter @basmat/shared build && pnpm --filter @basmat/server build && pnpm --filter @basmat/web build",
    "start": "pnpm --filter @basmat/server start",
    "test": "pnpm -r --no-bail run test",
    "typecheck": "pnpm -r --no-bail run typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "db:migrate": "pnpm --filter @basmat/server db:migrate",
    "db:seed": "pnpm --filter @basmat/server db:seed",
    "db:reset": "pnpm --filter @basmat/server db:reset"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "eslint": "^9.12.0",
    "@eslint/js": "^9.12.0",
    "typescript-eslint": "^8.8.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
